class Member {
    constructor(userName, firstName, lastName, groupId, job) {
        this.userName = userName;
        this.firstName = firstName;
        this.lastName = lastName;
        this.groupId = groupId;
        this.job =job;
    }
}

module.exports = Member;